
        import { StaticImageData } from "next/image";
        
        interface iImg {
            
            [key: string]: StaticImageData;
        };
        const img: iImg = { };
        export default img;
    